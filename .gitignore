# Build artifacts
*.ncb
*.suo
*.user
*.exe
*.xbe
*.obj
*.pch
*.pdb
*.map
*.log
*.wrn
*.ilk
*.idb
Debug/
Release/
Profile/
Profile_FastCap/
Release_LTCG/
obj/
ddk/
xdk/
build-env/

# Reference builds
UIX-Lite-Build/

# OS junk
.DS_Store
Thumbs.db

# IDE
.vscode/
.claude/
.mcp.json
ghidra/
ida/
binary-analysis/

# Desktop build artifacts
build-desktop/

# Local build staging and backup trees (not part of the public source)
build/git-clean/
build/thinkpad-patched/
build/bin-git/
build/build.log
build/Makefile.git-test
build/Makefile.thinkpad

# Backup files
*.bak
MediaDB.cache

# xiptool extract scratch dirs. Source-of-truth for xap files lives in
# xips-source/; Data/Xips/<name>/ only exists as a pack staging area
# (xiptool names .xip after its input folder) or as leftover from
# `xiptool extract`.
Data/Xips/default/
Data/Xips/default-xbox/

# bgfx build artifacts (Phase 2 of D3D audit). Submodule sources live
# at theseus/third-party/{bx,bimg,bgfx}/; their .build/ output is
# machine-specific and rebuilds via `make osx-arm64-release` etc.
theseus/third-party/bgfx/.build/
